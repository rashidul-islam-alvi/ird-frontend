import React from "react";
import SubCategory from "./sub-category";

const SubcatgoryWrapper = ({ catId }: { catId: number }) => {
  return <SubCategory catId={catId} />;
};

export default SubcatgoryWrapper;
